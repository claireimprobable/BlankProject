package improbable.tree;

enum TreeStatus {
  HEALTHY = 0;
  BURNY = 1;
  BALDY = 2;
  STUMPY = 3;
}

type IgniteRequest {}
type IgniteResponse {}

component TreeState {
  id = 1600;
  TreeStatus status = 1;
  command IgniteResponse ignite(IgniteRequest);
}